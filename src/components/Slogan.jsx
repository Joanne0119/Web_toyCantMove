import React from 'react'

const Slogan = ({ children }) => (
    <div className="w-full bg-gray-200 py-6 text-2xl text-center leading-relaxed">
        {children}
    </div>
);

export default Slogan
